<UserControl x:Class="DCS.TASK.NET.CommonControl.MesShow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <Grid>
        <DataGrid  Margin="0,5,0,0"
                   AutoGenerateColumns="False"
                   IsReadOnly="True"
                   CanUserAddRows="False"
                   Background="{StaticResource DefaultColor}">
            <DataGrid.Columns>
                <DataGridTextColumn Header ="接口名称" Binding ="{Binding Interfacename}" />
                <DataGridTextColumn Header ="调用时间" Binding ="{Binding UpdateTime}"/>
                <DataGridTextColumn Header ="接口调用结果" Binding ="{Binding Interfacecode}"/>
                <DataGridTextColumn Header ="返回信息" Binding ="{Binding Interfacemsg}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
